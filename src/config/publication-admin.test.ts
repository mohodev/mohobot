import{describe,expect,it}from'vitest';import{ConfigPublicationAdminAdapter}from'./publication-admin.js';import{ConfigPublicationStateMachine,InMemoryPublicationStateStore}from'./publication-state.js';describe('ConfigPublicationAdminAdapter',()=>{it('uses authenticated principal identity and exposes an admin-friendly snapshot',async()=>{const adapter=new ConfigPublicationAdminAdapter(new ConfigPublicationStateMachine(new InMemoryPublicationStateStore,()=>7));const result=await adapter.publish({expectedRevision:0,payload:{ok:true}},{id:'admin-1',role:'admin',enabled:true});expect(result.ok&&result.snapshot.active?.publishedBy).toBe('admin-1');const view=await adapter.get();expect(view.snapshot.state.activeRevision).toBe(1);expect(view.history).toHaveLength(1);expect(view.transitions.length).toBeGreaterThan(0)})});
