import{describe,expect,it}from'vitest';import{CacheMetric,LatencyMetric}from'./metrics.js';describe('metrics',()=>{it('computes bounded latency percentiles',()=>{const m=new LatencyMetric(3);[1,10,100,1000].forEach(x=>m.record(x));m.record(5,false);const s=m.summary();expect(s.count).toBe(3);expect(s.failures).toBe(1);expect(s.p95Ms).toBeGreaterThanOrEqual(s.p50Ms);});it('tracks cache ratio',()=>{const c=new CacheMetric();c.hit();c.miss();expect(c.snapshot().ratio).toBe(.5);});});
