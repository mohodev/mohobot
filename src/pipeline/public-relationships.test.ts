import fs from'node:fs/promises';import os from'node:os';import path from'node:path';import{afterEach,describe,expect,it}from'vitest';import{PublicRelationshipStore}from'./public-relationships.js';let root='';afterEach(async()=>{if(root)await fs.rm(root,{recursive:true,force:true});root='';});describe('PublicRelationshipStore',()=>{it('only exposes explicitly public named relations',()=>{const store=new PublicRelationshipStore([{leftId:'a',leftName:'阿澈',rightId:'b',rightName:'小林',relation:'close_friends',visibility:'public',updatedAt:'x'}]);expect(store.describe('阿澈和小林关系怎么样')).toBe('阿澈和小林关系很好');expect(store.describe('阿澈喜欢喝什么')).toBeUndefined();expect(new PublicRelationshipStore().describe('阿澈和小林关系怎么样')).toBeUndefined();});it('loads only a valid explicit roster and fails closed',async()=>{root=await fs.mkdtemp(path.join(os.tmpdir(),'moho-public-rel-'));const file=path.join(root,'data/relationships/public.json');await fs.mkdir(path.dirname(file),{recursive:true});await fs.writeFile(file,JSON.stringify([{leftId:'a',leftName:'阿澈',rightId:'b',rightName:'小林',relation:'friends',visibility:'public',updatedAt:'x'}]));expect((await PublicRelationshipStore.load(root)).describe('阿澈和小林')).toBe('阿澈和小林是朋友');await fs.writeFile(file,'{bad');expect((await PublicRelationshipStore.load(root)).describe('阿澈和小林')).toBeUndefined();});});
