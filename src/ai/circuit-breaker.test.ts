import{describe,expect,it}from'vitest';import{CircuitBreaker}from'./circuit-breaker.js';describe('CircuitBreaker',()=>{it('opens, allows one half-open request and closes on success',()=>{let now=0;const b=new CircuitBreaker({failureThreshold:2,openMs:100,now:()=>now});expect(b.acquire()).toBe(true);b.failure('network');expect(b.acquire()).toBe(true);b.failure('timeout');expect(b.snapshot().state).toBe('open');expect(b.acquire()).toBe(false);now=100;expect(b.acquire()).toBe(true);expect(b.acquire()).toBe(false);b.success();expect(b.snapshot().state).toBe('closed');});it('reopens after half-open failure',()=>{let now=0;const b=new CircuitBreaker({failureThreshold:1,openMs:10,now:()=>now});b.failure('server');now=10;expect(b.acquire()).toBe(true);b.failure('server');expect(b.snapshot().state).toBe('open');});});
