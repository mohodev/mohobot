import { describe, expect, it } from 'vitest'; import { OBSERVED_NVIDIA_FREE_MODELS, TASK_TUNING, recommend } from './model-catalog.js';
describe('model catalog',()=>{it('contains observed free endpoint capabilities',()=>{expect(OBSERVED_NVIDIA_FREE_MODELS.length).toBeGreaterThan(10);expect(OBSERVED_NVIDIA_FREE_MODELS.find(x=>x.id.includes('embed-1b'))?.capabilities).toContain('embedding');expect(OBSERVED_NVIDIA_FREE_MODELS.find(x=>x.id.includes('rerank'))?.capabilities).toContain('rerank');});it('recommends capability-matched free models',()=>{const rows=recommend({source:'x',fetchedAt:'',models:OBSERVED_NVIDIA_FREE_MODELS},'embedding');expect(rows.every(x=>x.capabilities.includes('embedding'))).toBe(true);expect(TASK_TUNING.world!.primaryCapability).toBe('world');});});
