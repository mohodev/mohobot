import{readFile}from'node:fs/promises';import{describe,expect,it}from'vitest';describe('中文单语言控制台',()=>{it('uses Chinese confirmation phrase and has no legacy login label',async()=>{const [html,js]=await Promise.all([readFile(new URL('../../webui/index.html',import.meta.url),'utf8'),readFile(new URL('../../webui/assets/app.js',import.meta.url),'utf8')]);expect(html).toContain('输入 <strong>确认</strong> 继续');expect(html).not.toContain('Legacy Bootstrap');expect(js).toContain("input.value === '确认'");expect(js).not.toContain("input.value === 'CONFIRM'");});});
